# While using function as an argument for another function, 
# we might need it only in one particular place
# If its rather easy, we can use LAMBDA. lambda  is and anonymous 
# function.
#     students.sort(key=lambda student: student.grades_avg())